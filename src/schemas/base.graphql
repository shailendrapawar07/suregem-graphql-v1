# type User {
#   id: ID!
#   email: String!
#   name: String!
# }

type Query {
  _empty: String
}

type Mutation {
  _empty: String
}

# Enums
enum UserType {
  MERCHANT
  RETAILER
}

# ========User Schema=========>
type User {
  id: ID!
  firstName: String
  lastName: String
  email: String!
  phone: String!
  address: String
  city: String
  state: String
  zip: String
  isEmailVerified: Boolean!
  isPhoneVerified: Boolean!
  updatedBy: String
  createdAt: String!
  updatedAt: String!
  userType: UserType
}
type UserResponse {
  id: String
  email: String
  phone: String
  isEmailVerified: Boolean!
  isPhoneVerified: Boolean!
  createdAt: String!
  updatedAt: String!
}
